"""HTTP-слой: единый контракт запросов, ответов и ошибок.

Пакет намеренно пуст: сервисы импортируют `app.api.errors`, а этот модуль
не должен при этом тянуть за собой роуты — иначе получается цикл
services → api → routes → services.
"""
