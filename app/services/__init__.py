"""Бизнес-логика шлюза.

Слой не знает про HTTP: не импортирует `Request`, не возвращает `Response` и не
формирует коды состояния. Он бросает доменные исключения из `app.api.errors` и
возвращает модели.

Пакет намеренно пуст — модули импортируются поимённо, чтобы не создавать цикл
через `app.api`.
"""
